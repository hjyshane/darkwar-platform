// database.types.ts is generated: `pnpm db:types` (SQL is the single source
// of truth; CI fails on drift). contracts.ts (Zod) joins in P2 alongside the
// Python Pydantic models, pinned by the shared protocol fixtures.
export type * from './database.types';
