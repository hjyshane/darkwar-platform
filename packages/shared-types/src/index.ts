// database.types.ts is generated: `pnpm db:types` (SQL is the single source
// of truth; CI fails on drift). contracts.ts is hand-written and pinned by
// the shared protocol fixtures next to the Python Pydantic models.
export * from './contracts';
export type * from './database.types';
